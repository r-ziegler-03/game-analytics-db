{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "DIR=\"$(pwd)/session-summaries\"; if [ -d \"$DIR\" ] && ls \"$DIR\"/*.md >/dev/null 2>&1; then CONTENT=$(cat \"$DIR\"/*.md); jq -n --arg ctx \"Summaries of prior sessions on this project:\n\n$CONTENT\" '{hookSpecificOutput: {hookEventName: \"SessionStart\", additionalContext: $ctx}}'; fi"
          }
        ]
      }
    ]
  }
}
